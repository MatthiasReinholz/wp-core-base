<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Downstream Premium Provider Registry
|--------------------------------------------------------------------------
|
| Register custom premium providers here.
|
| Example:
|
| return [
|     'example-vendor' => [
|         'class' => 'Project\\WpCoreBase\\Premium\\ExampleVendorManagedSource',
|         'path' => '.wp-core-base/premium-providers/example-vendor.php',
|     ],
| ];
|
| The `path` entry is optional when the class is autoloadable, but the
| scaffolded/default pattern is to keep the provider file in the downstream
| repo and let wp-core-base load it directly from `path`.
|
*/
return [];
